#執行程式。
npx expo start --clear 
npx expo start

#網頁版	
npx expo start --web



# GIT master
git checkout master

選項 A：如果你想把這些檔案傳上去 輸入 git add . 然後進行 commit。

1. 最簡單：確認「目前」在哪個分支
git branch

2. 看更詳細：包含遠端分支
git branch -a

確認目前所在分支git branch 或 git status
查看所有分支 (含遠端)git branch -a
切換到別的分支git checkout <分支名稱>
建立新分支並切換過去git checkout -b <新分支名稱>

git commit -m "你的修改說明"

